import { StyleSheet } from "react-native";

export default StyleSheet.create({
  bar: {
    width: 40,
    height: 10,
    marginRight: 4,
  },
  barContainer: {
    display: "flex",
    flexDirection: "row",
  },
  scoreContainer: {
    display: "flex",
    flexDirection: "row",
    marginBottom: 8,
  },
});
