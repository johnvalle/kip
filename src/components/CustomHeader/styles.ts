import { StyleSheet } from "react-native";

export default StyleSheet.create({
  headerContainer: {
    width: "90%",
    alignSelf: "center",
    backgroundColor: "transparent",
    paddingHorizontal: 0,
  },
});
